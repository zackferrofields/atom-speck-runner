module.exports = {
  // test > sum function
  // # sum(1,2) === 3 (returns the sum of both parameters)
  // # sum(3,-1) === 2 (works with negative numbers)
  sum: function(a,b) {
    return a + b;
  }
}
